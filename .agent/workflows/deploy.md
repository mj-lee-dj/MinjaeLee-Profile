---
description: Deploy changes to GitHub (triggers Vercel deployment)
---
// turbo-all
1. Add changes
   `git -C profile-site add .`

2. Commit changes
   `git -C profile-site commit -m "Update via Agent"`

3. Push changes
   `git -C profile-site push origin main`
